<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:background="@color/white"
        android:focusable="true"
        android:clickable="true"
        android:layout_height="match_parent"
        tools:context=".ui.search.SearchProductFragment">
        <ImageView
            android:id="@+id/image_left"
            android:padding="0dp"
            android:layout_marginTop="20dp"
            android:layout_marginStart="20dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:src="@drawable/ic_arrow_left"
            android:layout_width="50dp"
            android:layout_height="50dp"/>
        <TextView
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/image_left"
            app:layout_constraintBottom_toBottomOf="@id/image_left"
            style="@style/TextAppearance.AppCompat.Title"
            android:layout_marginStart="10dp"
            android:textColor="@color/black"
            android:text="Find Product"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <EditText
            android:id="@+id/editText"
            getTextChange="@{model.text}"
            app:layout_constraintTop_toBottomOf="@id/image_left"
            android:drawableStart="@drawable/ic_search_store"
            android:hint="Search Store"
            android:layout_marginHorizontal="20dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
        <androidx.recyclerview.widget.RecyclerView
            android:layout_marginTop="20dp"
            exploreDetailAdapter="@{adapterProduct}"
            app:layout_constraintTop_toBottomOf="@id/editText"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="20dp"
            android:layout_width="match_parent"
            android:layout_height="0dp"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
    <data>
        <variable
            name="model"
            type="com.example.projectgumi.viewmodel.SearchStoreViewModel" />
        <variable
            name="adapterProduct"
            type="com.example.projectgumi.adapter.ProductItemAdapter" />
    </data>
</layout>

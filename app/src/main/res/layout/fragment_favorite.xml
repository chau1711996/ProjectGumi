<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:background="@color/white"
        android:layout_height="match_parent"
        tools:context=".ui.favorite.FavoriteFragment">

        <View
            app:layout_constraintTop_toTopOf="@id/adapterMyFavorite"
            android:background="@color/search_store_bg"
            android:layout_width="match_parent"
            android:layout_height="1dp"/>
        <View
            app:layout_constraintTop_toBottomOf="@id/adapterMyFavorite"
            android:background="@color/search_store_bg"
            android:layout_width="match_parent"
            android:layout_height="1dp"/>

        <Button
            android:id="@+id/btnFavorite"
            style="@style/Widget.AppCompat.Button.Colored"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:background="@drawable/bg_button_green"
            android:text="@string/add_all_to_cart"
            android:textAllCaps="true"
            app:layout_constraintBottom_toBottomOf="parent" />
        <TextView
            android:id="@+id/textCartName"
            style="@style/TextAppearance.AppCompat.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="20dp"
            android:text="@string/favorite_name"
            android:textColor="@color/black"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/adapterMyFavorite"
            favoriteAdapter="@{adapterProduct}"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginHorizontal="20dp"
            android:layout_marginBottom="20dp"
            app:layout_constraintBottom_toTopOf="@id/btnFavorite"
            app:layout_constraintTop_toBottomOf="@id/textCartName" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>
        <variable
            name="adapterProduct"
            type="com.example.projectgumi.adapter.ProductItemAdapter" />
    </data>

</layout>
